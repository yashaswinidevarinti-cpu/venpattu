import React from 'react'
import ReactDOM from 'react-dom/client'
import VenpattuEcommerce from './venpattu-ecommerce.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VenpattuEcommerce />
  </React.StrictMode>,
)
